document.addEventListener('DOMContentLoaded', () => {
    const button = document.getElementById('ctaButton');
    button.addEventListener('click', () => {
        alert('Thank you for visiting my GitHub Page!');
    });
});
